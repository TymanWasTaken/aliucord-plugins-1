version = "1.0.1"
description =
    "Replaces dashes in channel names with spaces. This is likely to break on Discord updates!"

aliucord {
    changelog.set("""
       # 1.0.1
       * Fix error spam in logs - Tyman
       # 1.0.0
       * Initial release
    """.trimIndent())
    author("Tyman", 487443883127472129L)
}